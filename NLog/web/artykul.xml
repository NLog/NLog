<?xml version="1.0" encoding="windows-1250" ?>
<?xml-stylesheet type="text/xsl" href="style.xsl" ?>
<content lang="en" id="">
    <h1>Zaawansowane śledzenie aplikacji z wykorzystaniem NLog</h1>
    <p>Autor: Jarosław Kowalski &lt;jaak@jkowalski.net&gt;</p>
    <p>
        Niniejszy artykuł opisuje wykorzystanie biblioteki NLog do zarządzania informacjami 
        diagnostycznymi generowanymi przez aplikację w trakcie pracy.
        Śledzenie (inaczej logowanie) polega na zapisywaniu komunikatów 
        diagnostycznych (logów) tak, 
        aby możliwa była ich analiza na bieżąco lub po zakończeniu działania programu. 
        Zwykle komunikaty zapisywane są do pliku, lecz można też wykorzystywać inne 
        mechanizmy (systemowy dziennik zdarzeń, dołączony debugger, konsole
        zarządzania i inne).
    </p>
    <p>
        Śledzenie to bardzo przydatna technika pozwalająca przyjrzeć się pracy
        aplikacji zarówno podczas jej tworzenia jak i w środowisku produkcyjnym (gdzie 
        uruchomienie debuggera nie jest możliwe). 
    </p>
    <p>
        NLog pozwala w elastyczny sposób zarządzać informacjami diagnostycznymi pozwalając 
        decydować o takich aspektach logowania, jak:
    </p>
    <ul>
        <li><b>filtrowanie</b> - czyli decydowanie o tym, które logi generowane przez 
            aplikację powinny być zapisane a które odrzucone.</li>
        <li><b>formatowanie</b> - czyli określanie w jaki sposób informacje diagnostyczne
            powinny byś sformatowane i wzbogacone o dane kontekstowe.</li>
        <li><b>zapisywanie</b> - czyli określenie miejsca, do którego powinny być 
            zapisywane logi.</li>
    </ul>
    <h2>Źródła i poziomy komunikatów diagnostycznych</h2>
    <p>
        Przy korzystaniu z biblioteki NLog, każdy komunikat diagnostyczny ma określone  
        <b>źródło</b> i <b>poziom ważności</b>. Na podstawie źródła i poziomu ważności możliwe 
        jest filtrowanie i sterowanie przepływem komunikatów, dlatego istotne jest 
        zdefiniowanie i utrzymanie spójnej konwencji ich stosowania.
        Źródło najczęściej identyfikuje klasę, która emituje komunikaty diagnostyczne, ale nie 
        jest to bezwzględnie wymagane i możliwe są inne podejścia. 
    </p>
    <p>
        Określone są następujące poziomy ważności: 
    </p>
    <p>
        <div class="table">
            <table>
                <tr>
                    <th>Poziom</th>
                    <th>Opis</th>
                </tr>
                <tr>
                    <td valign="top">Trace</td>
                    <td valign="top">Komunikaty techniczne najniższego poziomu, potencjalnie bardzo liczne i 
                        zawierające duże ilości informacji, które mogą być interesujące tylko dla programisty. 
                        Te poziom ważności nie powinien być nigdy używany w środowisku produkcyjnym.</td>
                </tr>
                <tr>
                    <td valign="top">Debug</td>
                    <td valign="top">Komunikaty techniczne nieco wyższego poziomu niż Debug, które mogą 
                        pomóc administratorom i pracownikom działów wsparcia aplikacji w diagnozowaniu
                        problemów z jej działaniem.
                    </td>
                </tr>
                <tr>
                    <td valign="top">Info</td>
                    <td valign="top">Komunikaty informacyjne, zwykle niezbyt częste, informujące o zdarzeniach podczas 
                        poprawnego działania aplikacji. Przykład "<i>Użytkownik jkowalski został zalogowany</i>".
                        W środowisku produkcyjnym aplikacji jest to poziom ważności poniżej którego zwykle 
                        informacje nie są zapisywane, więc jeśli jakaś informacja musi być rejestrowana zawsze, należy
                        użyć tego poziomu ważności.</td>
                </tr>
                <tr>
                    <td valign="top">Warn</td>
                    <td valign="top">Ostrzeżenia, zwykle dość rzadkie. Ten poziom ważności powinien
                        być wykorzystywany do przekazywania informacji, na które powinni zwracać uwagę pracownicy
                        wsparcia technicznego. Pomimo wyemitowania ostrzeżenia, aplikacja działa dalej. 
                        Przykładowe ostrzeżenie: "<i>Zapytanie trwało zbyt długo: SELECT * FROM Tabela</i>"
                        </td>
                </tr>
                <tr>
                    <td valign="top">Error</td>
                    <td valign="top">Błędy, zwykle dość rzadkie. Ten poziom ważności powinien
                        być wykorzystywany do przekazywania informacji, na które powinni zwracać uwagę pracownicy
                        wsparcia technicznego</td>
                </tr>
                <tr>
                    <td valign="top">Fatal</td>
                    <td valign="top">Błędy krytyczne - zwykle powodujące niemożliwość dalszego działania aplikacji.
                    </td>
                </tr>
            </table>
        </div>
    </p>
    <h2>API</h2>
    <p>
        Aby aplikacja mogła wypisać komunikat diagnostyczny, musi pobrać obiekt reprezentujący 
        źródło i wołając odpowiednią metodę określić treść i poziom ważności komunikatu.
        Źródła reprezentowane są przez obiekty klasy <code>Logger</code> (zwane od tej
        pory loggerami). Klasa <code>Logger</code> udostępnia metody o nazwach takich 
        jak poziomy ważności, wywołanie jednej z takich metod oznacza wypisanie 
        komunikatu na określonym poziomie logowania.
    </p>
    <p>
        Tworzeniem loggerów zajmuje się <code>LogManager</code>. Interfejs programistyczny (API),
        który udostępnia NLog jest więc w założeniach podobny do interfejsu jaki
        udostępnia biblioteka log4net [1]. Interfejs NLoga jest w stosunku do log4net nieco rozszerzony i zoptymalizowany pod 
        względem wydajności, co zostanie omówione w dalszej części artykułu.
    </p>
    <p>
        Poniższy przykład pokazuje jak można utworzyć logger i wypisać za jego pośrednictwem komunikaty o 
        różnych poziomach ważności.
    </p>
    <cs inline="true" src="examples/article1.cs" />
    <h2>Konfiguracja logowania</h2>
    <p>
        Aby komunikaty diagnostyczne mogły być gdziekolwiek zapisane, należy 
        określić konfigurację logowania, która określi sposób filtrowania, formatowania i 
        zapisywania logów aplikacji. NLog konfiguruje się automatycznie na podstawie podstawie 
        tekstowego pliku konfiguracyjnego w formacie XML. W najprostszym scenariuszu plik ma
        nazwę <code>NazwaAplikacji.exe.nlog</code> lub <code>Web.nlog</code> dla aplikacji ASP.NET.
        Możliwości automatycznej konfiguracji jest znacznie więcej (w szczególności można wykorzystać plik
        <code>App.config</code>), wszystkie one są wymienione w dokumentacji projektu a część zostanie 
        wspomniana w dalszej części artykułu.
    </p>
    <p>
        Poniższy listing pokazuje najprostszą konfigurację logowania, która wypisuje 
        wszystkie komunikaty na standardowe wyjście procesu (w oknie konsoli) w układzie "data komunikat":
    </p>
    <pre class="csharp">
        2005-05-01 20:30:12.234 komunikat1
        2005-05-01 20:30:12.234 komunikat2
        2005-05-01 20:30:12.234 komunikat3
        2005-05-01 20:30:12.234 komunikat4
        2005-05-01 20:30:12.234 komunikat5</pre>
    <xml inline="true" src="examples/article3.nlog" />
    <p>
        Możemy wyróżnić dwie zasadnicze sekcje: <code>&lt;targets&gt;</code>
        oraz <code>&lt;rules&gt;</code>. Pierwsza definiuje wyjścia komunikatów diagnostycznych,
        druga steruje filtrowaniem i przepływem komunikatów diagnostycznych ze źródeł do
        wyjść.
    </p>
    <p>
        Widzimy zdefiniowane jedno wyjście o nazwie <code>konsola</code> typu <code>Console</code> z określonym
        formatem wypisywanej informacji <pre class="xml">${longdate} ${message}</pre>
    </p>
    <p>
        Jedyna reguła zawarta w elemencie <code>&lt;logger&gt;</code> nakazuje, aby wszystkie komunikaty
        o poziomie ważności <code>Debug</code> lub wyższym (czyli z wyłączeniem <code>Trace</code>)
        trafiały do wyjścia o nazwie <code>konsola</code>.
    </p>
    <h2>Dostępne wyjścia</h2>
    <p>
        NLog standardowo wspiera następujące rodzaje wyjść (wymieniono tylko najważniejsze):
    </p>

    <div class="table">
        <table>
            <tr>
                <th>Nazwa</th>
                <th>Opis</th>
            </tr>
            <tr>
                <td>ASPNetTrace</td>
                <td>Zapisuje komunikaty do ASP.NET Trace (dostępnej następnie przez stronę Trace.axd)</td>
            </tr>
            <tr>
                <td>Console</td>
                <td>Wypisuje komunikaty na standardowe wyjście (lub wyjście dla błędów) procesu</td>
            </tr>
            <tr>
                <td>Database</td>
                <td>Zapisuje komunikaty do bazy danych przy użyciu ADO.NET</td>
            </tr>
            <tr>
                <td>Debugger</td>
                <td>Wypisuje komunikaty do dołączonego debuggera kodu zarządzanego</td>
            </tr>
            <tr>
                <td>File</td>
                <td>Wypisuje komunikaty do pliku lub wielu plików</td>
            </tr>
            <tr>
                <td>Memory</td>
                <td>Wypisuje komunikaty do bufora w pamięci</td>
            </tr>
            <tr>
                <td>MessageBox</td>
                <td>Wyświetla kolejne komunikaty jako wyskakujące okienka dialogowe</td>
            </tr>
            <tr>
                <td>MethodCall</td>
                <td>Wywołuje wskazaną metodę</td>
            </tr>
            <tr>
                <td>Network</td>
                <td>Wysyła komunikat za pośrednictwem sieci z wykorzystaniem protokołu TCP, UDP lub HTTP</td>
            </tr>
            <tr>
                <td>EventLog</td>
                <td>Zapisuje komunikaty do systemowego dziennika zdarzeń</td>
            </tr>
            <tr>
                <td>MSMQ</td>
                <td>Zapisuje komunikaty do kolejki komunikatów MSMQ</td>
            </tr>
            <tr>
                <td>NLogViewer</td>
                <td>Wysyła komunikat do aplikacji NLogViewer za pośrednictwem sieci.</td>
            </tr>
            <tr>
                <td>Chainsaw</td>
                <td>Wysyła komunikat do aplikacji log4j Chainsaw za pośrednictwem sieci.</td>
            </tr>
            <tr>
                <td>Null</td>
                <td>Ignoruje komunikaty</td>
            </tr>
            <tr>
                <td>Trace</td>
                <td>Wysyła komunikaty za pośrednictwem <code>System.Diagnostics.Trace</code></td>
            </tr>
        </table>
    </div>
    <p>
        Listę tę można bardzo łatwo rozszerzyć definiując własne wyjście i rejestrując je w systemie,
        co jest operacją bardzo prostą, wymagającą zwykle napisania tylko kilkunastu linii kodu klasy dziedziczącej
        z klasy <code>NLog.Target</code>. Szczegółowe informacje
        na temat tworzenia własnego wyjścia podano na stronie projektu [2].
    </p>
    <h2>Formatowanie komunikatów</h2>
    <p>
        Podczas zapisywania komunikatu diagnostycznego do wyjścia możliwe jest wzbogacenie go
        o różnego rodzaju informacje kontekstowe. Możemy na przykład chcieć, aby każdy
        wiersz w pliku zawierał opróćz komunikatu także datę, poziom ważności komunikatu i
        identyfikator wątku, z którego pochodzi komunikat. Nic prostszego - wystarczy w parametrze 
        <code>layout</code> do wyjścia napisać:
    </p>
    <pre class="xml">${longdate} ${level} ${threadid} ${message}</pre>
    <p>
        A gdybyśmy chcieli wyświetlić także wartość wskazanej zmiennej środowiskowej,
        licznika wydajności (performance counter) lub wartości zmiennej przechowywanej w sesji ASP.NET lub ASP?
        Jest to równie proste. Odpowiednie "zaklęcia" to:
    </p>
    <pre class="xml">${environment:variable=VAR}</pre>
    <pre class="xml">${performancecounter:category=...:counter=...:instance=...}</pre>
    <pre class="xml">${aspnet-session:variable=UserName}</pre>
    <pre class="xml">${asp-session:variable=UserName}</pre>
    <p>
        Informacje kontekstowe możemy wykorzystać nie tylko do formatowania tekstu wyjścia, w wielu przypadkach
        możliwe jest sterowanie parametrami samego wyjścia. Przykładowo, aby logi z każdego dnia pracy aplikacji
        trafiały do osobnego pliku wystarczy użyć informacji kontekstowych w parametrze <code>filename</code>
        ustawiającym nazwę pliku:
    </p>
    <xml inline="true" src="examples/article4.nlog" />
    <p>
        Lista informacji kontekstowych, które można wydobyć przy pomocy NLog to:
    </p>
    <div class="table">
        <table>
            <tr>
                <th>Nazwa</th>
                <th>Opis</th>
            </tr>
            <tr>
                <td>asp-application</td>
                <td>Zmienna przechowywana w konteście aplikacji ASP (klasyczne).</td>
            </tr>
            <tr>
                <td>asp-request</td>
                <td>Parametr wejściowy żądania ASP (klasyczne)</td>
            </tr>
            <tr>
                <td>asp-session</td>
                <td>Zmienna sesyjna aplikacji ASP (klasyczne)</td>
            </tr>
            <tr>
                <td>aspnet-application</td>
                <td>Zmienna przechowywana w konteście aplikacji ASP.NET.</td>
            </tr>
            <tr>
                <td>aspnet-request</td>
                <td>Parametr wejściowy żądania ASP.NET</td>
            </tr>
            <tr>
                <td>aspnet-session</td>
                <td>Zmienna sesyjna aplikacji ASP.NET</td>
            </tr>
            <tr>
                <td>basedir</td>
                <td>Katalog bazowy aplikacji</td>
            </tr>
            <tr>
                <td>callsite</td>
                <td>Informacja o klasie, metodzie, nazwie pliku, numerze wiersza i kolumny z miejsca wywołania</td>
            </tr>
            <tr>
                <td>counter</td>
                <td>Licznik - generuje rosnące ciągi liczbowe</td>
            </tr>
            <tr>
                <td>date</td>
                <td>Data i czas w dowolnym formacie</td>
            </tr>
            <tr>
                <td>environment</td>
                <td>Wartość wskazanej zmiennej środowiskowej</td>
            </tr>
            <tr>
                <td>exception</td>
                <td>Informacje o wyjątku związanym z komunikatem diagnostycznym</td>
            </tr>
            <tr>
                <td>guid</td>
                <td>Globally-unique identifier (GUID) - losowa liczba 128-bitowa unikalna w czasie i przestrzeni</td>
            </tr>
            <tr>
                <td>identity</td>
                <td>Informacja o tożsamości wątku pochodząca z obiektu <code>IIdentity</code> związanego z bieżącym wątkiem</td>
            </tr>
            <tr>
                <td>level</td>
                <td>Poziom ważności komunikatu</td>
            </tr>
            <tr>
                <td>logger</td>
                <td>Nazwa loggera (źródła komunikatów)</td>
            </tr>
            <tr>
                <td>longdate</td>
                <td>Data i czas w formacie <code>yyyy-MM-dd HH:mm:ss.mmm</code>, dogodnym do sortowania</td>
            </tr>
            <tr>
                <td>machinename</td>
                <td>Nazwa komputera, na którym działa aplikacja</td>
            </tr>
            <tr>
                <td>mdc</td>
                <td>Element kontekstu MDC związanego z bieżącym wątkiem (dla kompatybilności z log4net)</td>
            </tr>
            <tr>
                <td>ndc</td>
                <td>Element kontekstu NDC związanego z bieżącym wątkiem (dla kompatybilności z log4net)</td>
            </tr>
            <tr>
                <td>message</td>
                <td>Sformatowany komunikat diagnostyczny</td>
            </tr>
            <tr>
                <td>newline</td>
                <td>Znak końca wiersza</td>
            </tr>
            <tr>
                <td>nlogdir</td>
                <td>Katalog, w którym zainstalowano NLog.dll</td>
            </tr>
            <tr>
                <td>performancecounter</td>
                <td>Wartość licznika wydajności</td>
            </tr>
            <tr>
                <td>processtime</td>
                <td>Czas działania procesu w formacie <code>HH:mm:ss.mmm</code></td>
            </tr>
            <tr>
                <td>shortdate</td>
                <td>Data w krótkim formacie <code>yyyy-MM-dd</code> dogodnym do sortowania i użycia w nazwie pliku</td>
            </tr>
            <tr>
                <td>stacktrace</td>
                <td>Stos wywołań</td>
            </tr>
            <tr>
                <td>threadid</td>
                <td>Identyfikator bieżącego wątku</td>
            </tr>
            <tr>
                <td>threadname</td>
                <td>Nazwa bieżącego wątku</td>
            </tr>
            <tr>
                <td>ticks</td>
                <td>Wartość <code>Ticks</code> bieżącej daty.</td>
            </tr>
            <tr>
                <td>registry</td>
                <td>Wartość przechowywana w rejestrze</td>
            </tr>
            <tr>
                <td>windows-identity</td>
                <td>Nazwa użytkownika Windows związana z bieżącym wątkiem</td>
            </tr>
        </table>
    </div>
    <p>
        Podobnie jak w przypadku wyjść możliwe jest łatwe dodawanie nowej funkcjonalności pobierania
        informacji kontekstowych, wystarczy napisać własną klasę dziedziczącą z klasy <code>NLog.LayoutRenderer</code>, 
        co jest również szczegółowo opisane na stronie projektu [2].
    </p>
    <h2>Filtrowanie komunikatów</h2>
    <p>
        Jak już wspomniano wyżej, jednym z głównych zadań biblioteki NLog jest filtrowanie komunikatów diagnostycznych.
        W odróżnieniu od log4net, które stosuje konfigurację hierarchiczną w NLogu zastosowano koncepcję podobną do tabeli
        routingu. Dla każdego komunikatu przeglądana jest tabela w poszukiwaniu reguły pasującej do nazwy źródła i
        poziomu ważności komunikatu i w przypadku pasującej wiadomości, jest ona zapisywana do wskazanych wyjść.
    </p>
    <p>
        Każdy wpis w "tabeli routingu komunikatów" składa się z 5 elementów. Są to:
        <ol>
            <li>Nazwa loggera lub wzorzec nazwy z użyciem symboli wieloznacznych (*)</li>
            <li>Poziom (lub poziomy) ważności komunikatów</li>
            <li>Dodatkowe filtry sprawdzające elementy inne niż nazwa loggera i poziom ważności</li>
            <li>Nazwy wyjść, do których ma być zapisany pasujący komunikat</li>
            <li>Informacja o tym, czy dalsze reguły mają być sprawdzane</li>
        </ol>
    </p>
    <p>
        Załóżmy, że mamy następującą zdefiniowaną następującą konfigurację wyjść (zdefiniowaliśmy dwa wyjścia plikowe, jedno na standardowe wyjście i jedno do bazy danych MSSQL):
    </p>
    <xml inline="true" src="examples/article5.nlog" />
    <p>
        Wyobraźmy sobie, że chcemy zaimplementować następujące reguły logowania:
    </p>
    <ol>
        <li>Błędy i ostrzeżenia (Warn, Error ale nie Fatal) muszą trafiać do pliku <code>errors.log</code> i do bazy danych</li>
        <li>Wszystkie informacje i błędy w komponencie Gizmo mają trafiać do bazy danych.</li>
        <li>Wszystkie komunikaty informacyjne i ważniejsze (Info, Warn, Error, Fatal) mają trafiać do pliku o nazwie takiej jak bieżąca data</li>
        <li>Komunikaty z komponentu FooBar niezależnie od poziomu ważności mają być ignorowane</li>
    </ol>
    <xml inline="true" src="examples/article6.nlog" />
    <p>
        Powyższa sekcja <code>&lt;rules&gt;</code> realizuje te wszystkie założenia. 
        Wydaje się, że jest ona dość czytelna, ale dla porządku prześledźmy:
        <ol>
            <li>Reguła numer 4 jest zapisana jako pierwsza. Mówi ona, że komunikaty, których źródło 
                pasuje do wzorca <code>FooBar*</code> (<code>name="FooBar*"</code>) na dowolnym poziomie (<code>minlevel="Trace"</code>)
                nie mają być nigdzie zapisywane (<code>writeTo=""</code>) i dalsze reguły mają być ignorowane na co wskazuje <code>final="true"</code>. 
                Reguła #4 musiała być zapisana na początku, gdyż zapewnia to jej wpływ na wszystkie komunikaty i umożliwia odrzucenie niektórych z nich 
                jeszcze przed zastosowaniem pozostałych reguł.</li>
            <li>Reguła numer 1 jest zapisana jako druga. Mówi ona, że dla wskazanych dwóch poziomów ważności (<code>levels="Warn,Error"</code>)
                komunikaty mają być zapisane do bazy danych i pliku errors.log (<code>writeTo="bledy,bazaDanych"</code>)</li>
            <li>W kolejnym wierszu zapisano regułę numer 2. Mówi ona, że komunikaty na poziomie powyżej Info (<code>minlevel="Info"</code>) dla komponentu
                Gizmo* (<code>name="Gizmo*"</code>) trafiają do bazy danych (<code>writeTo="bazaDanych"</code>)</li>
            <li>Przedostatni wiersz to implementacja reguły nr 3. Jest ona podobna do reguły 2, z wyjąkiem innego wzorca
                nazwy źródła (<code>name="*"</code>) i innej specyfikacji wyjścia <code>writeTo="codziennyPlik"</code>)</li>
            <li>Ostatni wiersz to implementacja reguły nr 5. Wszystkie komunikaty na poziomie Trace lub wyższym trafiają
            na konsolę.</li>
        </ol>
    </p>
    <h2>Przydatne linki</h2>
    <ol>
        <li>Projekt log4net - <a href="http://logging.apache.org/log4net">http://logging.apache.org/log4net</a></li>
        <li>Strona główna projektu - <a href="http://nlog.sourceforge.net/">http://nlog.sourceforge.net/</a></li>
        <li>Strona projektu SF.NET - <a href="http://sourceforge.net/projects/nlog/">http://sourceforge.net/projects/nlog/</a></li>
        <li>Blog autora, gdzie co jakiś czas publikowane są informacje o zmianach w NLogu - <a href="http://blog.jkowalski.net/">http://blog.jkowalski.net/</a></li>
        <li>Interfejs TRAC do zgłaszania błędów w NLogu - <a href="http://trac.sav.net/nlog">http://trac.sav.net/nlog</a></li>
    </ol>
</content>
