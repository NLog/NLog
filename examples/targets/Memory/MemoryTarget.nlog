<?xml version="1.0" ?>
<nlog>
    <targets>
        <target name="memory" type="Memory" layout="${message}" />
    </targets>

    <rules>
        <logger name="*" minLevel="Info" writeTo="memory" />
    </rules>
</nlog>
