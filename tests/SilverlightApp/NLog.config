<nlog>
  <targets>
    <target name="t" type="MessageBox" layout="installstate: ${sl-appinfo:InstallState} ${message}" />
  </targets>
  <rules>
    <logger name="*" writeTo="t" />
  </rules>
</nlog>
